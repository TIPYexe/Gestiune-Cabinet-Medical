import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.util.*;

public class test {
    Scanner sc = new Scanner(System.in);
    String dataFormat = sc.nextLine();

    public static void main(String[] args) throws ParseException {
        Scanner sc = new Scanner(System.in);
        String dataFormat = sc.nextLine();
        String[] test = dataFormat.split("-", 0);
        //Date data_programare = new SimpleDateFormat("dd/MM/yyyy").parse(Integer.toString(test[0]) + '/' + Integer.toString(luna) + '/' + Integer.toString(an));
        //System.out.println(data_programare);
    }
}
